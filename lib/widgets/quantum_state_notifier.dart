import 'package:flutter/material.dart';
class QuantumStateNotifier extends StatelessWidget {
  const QuantumStateNotifier({super.key});
  @override
  Widget build(BuildContext context) => const Icon(Icons.shield, color: Colors.greenAccent, size: 14);
}