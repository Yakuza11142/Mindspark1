import 'package:flutter/material.dart';
class SttVideoController extends StatelessWidget {
  const SttVideoController({super.key});
  @override
  Widget build(BuildContext context) => const Icon(Icons.mic_external_on, color: Colors.amber);
}