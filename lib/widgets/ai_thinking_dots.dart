import 'package:flutter/material.dart';
class AiThinkingDots extends StatelessWidget {
  const AiThinkingDots({super.key});
  @override
  Widget build(BuildContext context) => const Text("...", style: TextStyle(fontSize: 24, color: Colors.cyanAccent, letterSpacing: 2));
}