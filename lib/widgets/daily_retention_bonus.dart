import 'package:flutter/material.dart';
class DailyRetentionBonus extends StatelessWidget {
  const DailyRetentionBonus({super.key});
  @override
  Widget build(BuildContext context) => const Text("+50 Sparks Daily Bonus Available!", style: TextStyle(color: Colors.amber));
}