class ClassroomOfflineLeaderboard {
  static void broadcastScoreToNearbyPhones(int score) {
    // Uses Google Nearby Connections API to beam scores to classmates in the same room.
    print("Broadcasting score: $score to 14 nearby devices.");
  }
}