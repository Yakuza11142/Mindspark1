name: m1
on: [workflow_dispatch]
jobs:
  download_and_run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: curl -sL https://githubusercontent.com > .github/workflows/m1.yml && echo "Clean template injected!"
