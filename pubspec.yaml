name: MIND SPARK
description: AI TUTOR & AR LAB
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 1. INTELLIGENCE (Gemini + OpenAI)
  google_generative_ai: ^0.4.0
  dart_openai: ^5.1.0

  # 2. MONEY (Ads + IAP)
  google_mobile_ads: ^5.0.0
  in_app_purchase: ^3.1.13

  # 3. SENSES (Visuals + Audio)
  dio: ^5.4.0
  http: ^1.2.0
  audioplayers: ^5.2.1
  video_player: ^2.8.2
  cached_video_player: ^2.0.4
  image_picker: ^1.0.7
  model_viewer_plus: ^1.4.0  # AR/3D Engine

  # 4. UTILITIES
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  flutter_animate: ^4.5.0
  glassmorphism: ^3.0.0
  path_provider: ^2.1.2
  pdf: ^3.10.0
  flutter_email_sender: ^6.0.1
  intl: ^0.19.0
  dash_chat_2: ^0.0.20
  url_launcher: ^6.2.5
  simple_gradient_text: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
dependencies:
  # ... existing dependencies ...
  flutter_secure_storage: ^9.0.0 # ENCRYPTION (Anti-Hack)
dependencies:
  # ... existing dependencies ...
  flutter_secure_storage: ^9.0.0 # ENCRYPTION (Anti-Hack)
dependencies:
  # ... existing deps ...
  speech_to_text: ^6.6.0  # NEW: Allows the app to hear
  permission_handler: ^11.3.0 # To ask "Can I use your Microphone?"
dependencies:
  # ... existing deps ...
  url_launcher: ^6.2.5          # To open "Contact Support" or "Privacy Policy"
  flutter_local_notifications: ^17.0.0 # Daily Reminders (The Nagging)
  share_plus: ^9.0.0            # "Share App" button
  image_picker: ^1.0.7          # Profile Picture
dependencies:
  # ... existing ...
  screen_brightness: ^0.2.2  # To control light
  vibration: ^1.8.4          # To control frequency
dependencies:
  # ... existing deps ...
  url_launcher: ^6.2.5
dependencies:
  flutter_dotenv: ^5.1.0
  supabase_flutter: ^2.0.0

flutter:
  assets:
    - .env
dependencies:
  google_mlkit_language_id: ^0.10.0
dependencies:
  google_translate_api: ^1.0.0
dependencies:
  dependencies:
  qr_flutter: ^4.1.0          # Generates the QR
  ticket_widget_plus: ^1.0.1  # Gives the "punched-out" receipt look
  crypto: ^3.0.3              # Used for hashing/legitimacy verification
dependencies:
  ticket_clippers: ^1.1.0 # For the professional ticket shape
  qr_flutter: ^4.1.0      # For the high-quality QR code
  intl: ^0.19.0           # For clean date and time formatting
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat these warnings as "Fatal Errors" that kill the build
    unused_local_variable: error
    invalid_use_of_visible_for_testing_member: error
    missing_required_param: error
    dead_code: error
dev_dependencies:
  # Allows the app to inspect its own code health
  custom_lint: ^0.6.4 
dependencies:
  arcore_flutter_plus: ^1.0.2  # The recommended library for Android AR
  vector_math: ^2.1.0          # Required for positioning objects
flutter_markdown: ^0.7.1
dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  arcore_flutter_plus: ^1.0.2
dependencies:
  # The NEW ones for face recognition:
  google_ml_kit_face_detection: ^0.11.0  # Detects where the face is
  tflite_flutter: ^0.10.0               # The "Brain" that remembers the face
  image: ^4.1.3                         # Processes camera frames for the brain
dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  arcore_flutter_plus: ^1.0.2
  vector_math: ^2.1.0
  google_ml_kit_face_detection: ^0.11.0 # The "Face Finder"
  tflite_flutter: ^0.10.0               # The "Face Recognizer"
dependencies:
  biometric_storage: ^5.0.1
  encrypt: ^5.0.3
dev_dependencies:
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_icon.png"
  adaptive_icon_background: "#000000" # Black background to match your logo
  adaptive_icon_foreground: "assets/images/app_icon.png"
dependencies:
  flutter_dotenv: ^5.1.0

flutter:
  assets:
    - .env
flutter:
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
supabase_flutter: ^2.3.1
dependencies:
  shared_preferences: ^2.2.2
dependencies:
  ar_flutter_plugin_external: ^0.0.1
dependencies:
  sensors_plus: ^6.1.0 

flutter:
  assets:
    - assets/images/

dependencies:
  mobile_scanner: ^5.1.0
  audioplayers: ^6.0.0

dependencies:
  llm_dart: ^0.10.0
dependencies:
  crypto: ^3.0.3        # For SHA-3
  pointycastle: ^3.9.1  # For Post-Quantum math

dependencies:
  supabase_flutter: ^2.6.0
  pqcrypto: ^1.1.0 # The ML-DSA Verifier

dependencies:
  safe_device: ^1.1.4
  flutter_jailbreak_detection: ^1.1.1

dependencies:
  pqcrypto: ^1.1.0    # Post-Quantum ML-DSA (Dilithium)
  thirds: ^1.0.1      # FIPS-compliant SHA-3 (Keccak)

dependencies:
  groq_sdk: ^0.0.3

http: ^1.2.0

dependencies:
  supabase_flutter: ^2.6.0 # The latest stable version for 2026

dependencies:
  # ... existing deps ...
  supabase_core: ^2.24.2
  supabase_auth: ^4.16.0
  cloud_client: ^4.14.0
  supabase_remote_config: ^4.3.8
  supabase_analytics: ^10.8.0
  supabase_crashlytics: ^3.4.8
  supabase_messaging: ^14.7.8
  google_sign_in: ^6.1.6

dev_dependencies:
  test: ^1.24.0
  mockito: ^5.4.0
  http: ^1.1.0

dependencies:
  flutter:
    sdk: flutter
  # Latest stable version as of May 2026
  flutter_fortune_wheel: ^1.3.2 

dependencies:
  flutter:
    sdk: flutter
  # Latest stable version as of May 2026
  flutter_tex: ^5.2.6 
