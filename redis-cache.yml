version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: mindspark_redis_cache
    command: redis-server --maxmemory 2gb --maxmemory-policy allkeys-lru
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
volumes:
  redis_data: